# Core. Install torch first, matched to your CUDA runtime:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
torch>=2.4

# Triton. On Linux, Triton ships inside the torch wheel and this line is a no-op.
# On Windows it does not, so install the community build:
#   pip install triton-windows
triton-windows>=3.8 ; platform_system == "Windows"

pytest>=7.0

# Optional Linux-only baselines (see README section 1 -- none of these have
# Windows wheels, which is why the WSL2 path exists):
#   pip install flash-attn --no-build-isolation
#   pip install vllm
